# schotter
# Schotter Generative Art
Reproduce Georg Nees' classic *Schotter* generative artwork.

- Change `SEED` value inside sketch.py to generate different patterns.
- Run command `python3 sketch.py` in terminal to regenerate sketch.svg.
